function joinStrings(strings: string[]): string {
    return strings.join("");
}

console.log(joinStrings(["Hello, ", "World!", " This ", "is ", "TypeScript."]));

console.log(joinStrings(["One", "Two", "Three"]));
